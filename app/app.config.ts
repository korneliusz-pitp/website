export default defineAppConfig({
  ui: {
    // Semantic color configuration mapping PITP brand colors
    colors: {
      primary: "cannon",
      secondary: "beck",
      neutral: "neutral",
    },
  },
});
